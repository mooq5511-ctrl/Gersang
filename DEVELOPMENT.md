# 《Gersang》開發規範

> 本文件是《Gersang》網頁放置 RPG 的共同開發手冊。目標是讓每一次修改都容易理解、測試、回復與發布。

## 1. 專案概覽

《Gersang》是一款以繁體中文（`zh-TW`）為主要語言的單人網頁放置 RPG。玩家可建立角色、招募傭兵、探索世界地圖、進行即時地下城戰鬥、交易、收集裝備並保存進度。

- 技術：React、TypeScript、Vinext／Vite、pnpm。
- 遊戲資料主要存於瀏覽器 `localStorage`，目前沒有伺服器帳號或雲端存檔。
- 主要入口：`app/page.tsx` → `app/game-v15.tsx`。
- 遊戲存檔與遷移邏輯屬於相容性關鍵功能；變更前必須考慮既有玩家存檔。

常用指令：

```bash
pnpm dev          # 本機開發
pnpm lint         # 程式檢查
pnpm build        # 正式建置
node --test tests/*.test.mjs  # 自動測試
```

## 2. Git 工作流程

每一個 commit 都應是一個可以理解、可以測試、可安全回退的小單位。完成一項可驗證的工作後就 commit，不要把數天的不同工作混在一起。

基本流程：

```text
確認目前分支與變更
→ 實作一項小功能或修正
→ 執行相應測試與建置檢查
→ 撰寫清楚的 commit
→ Push 到 GitHub
```

範例：新增漢陽 NPC 的任務資料後，先確認對話、任務完成條件與重新載入存檔，再建立：

```text
feat: 新增漢陽藥師任務
```

不要 commit：`.env*`、Token、憑證、`node_modules/`、`.next/`、`dist/` 或其他建置產物。提交前先查看變更清單，確保不會把暫存檔或玩家資料加入版本控制。

## 3. Commit 訊息規範

格式：

```text
<type>: <簡短且具體的繁體中文描述>
```

| 類型 | 使用時機 | 範例 |
| --- | --- | --- |
| `feat` | 玩家可感受到的新功能 | `feat: 新增漢陽 NPC 對話系統` |
| `fix` | 修正錯誤或不符合預期的行為 | `fix: 修正地下城重載後獎勵重複發放` |
| `art` | 美術、音效、視覺或 UI 素材調整 | `art: 更新白虎林怪物圖示` |
| `balance` | 數值、機率、獎勵或戰鬥平衡調整 | `balance: 降低白虎領地怪物攻擊力` |
| `refactor` | 不改變遊戲效果的程式整理 | `refactor: 拆分怪物掉落資料來源` |
| `chore` | 工具、設定、文件或維護工作 | `chore: 更新 Git 忽略規則` |

描述要說明「做了什麼」，避免 `update`、`修正問題`、`測試` 這類沒有資訊的訊息。若一個變更同時包含新功能與美術，應優先分成兩個 commit；無法拆分時以主要目的決定類型。

## 4. 分支策略

目前以個人開發為主時，可在 `main` 完成小型、已驗證的修改。大型功能、風險較高的存檔調整，或需要長時間開發的工作應先建立分支。

```text
main                         # 可正常建置、可遊玩的穩定版本
feature/npc-quest-system     # 新系統
fix/dungeon-reward-once      # Bug 修正
refactor/monster-data        # 結構整理
codex/formation-editor       # 由 Codex 建立的工作分支
```

分支完成後，先更新至最新 `main`、解決衝突、跑完必要檢查，再合併回 `main`。不要在同一分支同時進行無關的戰鬥、UI、美術與工具改動。

## 5. 版本規範（SemVer）

版本採用語意化版本：`v主版本.次版本.修正版`。

```text
v0.4.0  # 加入可玩的 NPC、對話與任務系統
v0.4.1  # 修正 NPC 任務獎勵或存檔問題
v0.5.0  # 加入傭兵編隊與 AI 等大型功能
v1.0.0  # 首個正式穩定版
```

- **主版本（major）**：正式版後出現不相容的重大改動，例如存檔格式無法自動遷移。
- **次版本（minor）**：向下相容的大型新功能、區域或系統。
- **修正版（patch）**：向下相容的 Bug 修正、小型平衡與安全修正。

在 Alpha 階段從 `v0.x.y` 起算。只要存檔結構改動，必須提供遷移與舊存檔驗證；不能只提高版本號。

## 6. 發布流程

1. 確認本次版本的範圍與版本號。
2. 執行 `pnpm lint`、相關測試、完整測試與 `pnpm build`。
3. 進行手動遊玩檢查：建立／載入角色、傭兵與編隊、戰鬥、掉落、裝備、重新載入存檔。
4. 更新 `README.md` 或發布說明，列出新功能、修正與已知限制。
5. 建立發布 commit，例如 `chore: 準備 v0.4.0 發布`。
6. 在已驗證的 `main` 建立 Git Tag，例如 `v0.4.0`，Push 後建立 GitHub Release。

Release 標題範例：`Gersang v0.4.0 — NPC 與任務更新`。發布說明應包含玩家看得到的改變、重要修正、存檔相容性，以及任何已知問題。

## 7. 資料夾與檔案結構

| 位置 | 用途與原則 |
| --- | --- |
| `app/` | 遊戲畫面、React 元件、遊戲規則與流程；避免讓 UI 元件同時擁有大量資料定義。 |
| `data/` | 可維護的遊戲資料來源，例如怪物、NPC、地圖、物品、技能與契約；新內容優先資料驅動。 |
| `components/ui/` | 共用 UI 基礎元件；除非必要，不大量修改自動產生的元件。 |
| `public/assets/` | 實際使用的圖片、音樂、角色、怪物與裝備素材。 |
| `public/assets/placeholders/` | Alpha 階段尚未完成美術時的替代素材。 |
| `tests/` | 可獨立執行的遊戲規則、存檔遷移與回歸測試。 |
| `scripts/` | 資產整理或一次性維護工具；不可將秘密資訊寫入腳本。 |
| `AI_HANDOFF.md` | 交給 AI 協作者的現況、架構與相容性提醒；重大架構改動後一併更新。 |

新功能的推薦順序是：先在 `data/` 定義內容與 ID，再在 `app/` 實作規則／UI，最後在 `tests/` 補上規則與存檔保護。圖片引用應集中在資料或視覺對照層，避免散落在多個戰鬥元件中。

## 8. 命名規範

### 程式碼

- React 元件檔使用 `kebab-case.tsx`：`npc-dialogue.tsx`、`battle-arena.tsx`。
- 一般 TypeScript 模組使用 `kebab-case.ts`：`dungeon-engine.ts`、`vitals-engine.ts`。
- React 元件、型別與介面使用 `PascalCase`：`DungeonPanel`、`GameState`。
- 變數、函式與事件處理器使用 `camelCase`：`calculateReward`、`handleRecruitMercenary`。
- 布林值以 `is`、`has`、`can`、`should` 開頭：`isDungeonBusy`、`hasRewarded`。
- 穩定資料 ID 使用小寫 `kebab-case`，一旦發布不要任意改名：`e-white-tiger-king`、`hanyang-apothecary`。

### 資產與資料

- 圖片檔使用小寫 `kebab-case`，描述用途：`white-tiger-king.webp`、`hanyang-apothecary-portrait.webp`。
- 版本或來源需要區分時使用清楚後綴：`pirate-king-idle.webp`、`pirate-king-attack.webp`。
- 不使用 `final-final.png`、`新圖片.png` 或無意義流水號。
- 為每項新資產記錄可替換路徑與授權／來源；未確認最終素材時使用 placeholder，不讓美術阻礙遊戲功能。

### NPC 與遊戲內容

NPC 的穩定 ID 使用「地點＋角色」：`hanyang-apothecary`、`edo-shipwright`。顯示名稱可使用繁體中文，例如「漢陽藥師」；程式碼不得以顯示名稱作為唯一識別。任務 ID 使用 NPC 或區域前綴，例如 `hanyang-apothecary-herb-request`。

## 9. AI 協作流程

AI 是協作者，不是存檔與遊戲規則的唯一決策者。每次請 AI 修改前，先提供目標、影響範圍、現有行為與不可破壞條件。

1. **說明任務**：例如「新增漢陽藥師的可重複任務；不要改變既有存檔 key。」
2. **先讀情境**：AI 應先閱讀 `AI_HANDOFF.md`、相關資料檔、現有測試與相依元件。
3. **限定改動**：要求列出會修改的檔案；不要讓 AI 順手重寫不相關模組。
4. **保護相容性**：任何 `localStorage`、`GameState`、裝備槽、傭兵上限或戰鬥流程改動，先設計遷移與回退方式。
5. **驗證與交接**：AI 應回報實際修改、執行的檢查、結果與已知風險；重大架構變更更新 `AI_HANDOFF.md`。

給 AI 的任務範例：

```text
在 data/npcs/hanyang.ts 新增漢陽藥師任務資料，並在既有對話 UI 顯示。
保留現有 localStorage key 與舊任務進度；新增或更新針對獎勵只發放一次的測試。
完成後執行相關測試與 pnpm build，回報檔案與結果。
```

永遠不要把 API Key、密碼、Token 或完整玩家存檔交給 AI。對 AI 產生的改動，仍須依下列檢查表人工確認。

## 10. Code Review 檢查表

- [ ] 變更只處理預定問題，沒有混入不相關的格式化或重構。
- [ ] Commit 類型與描述符合規範，且容易從歷史紀錄理解。
- [ ] 新功能有清楚的資料 ID，沒有以顯示文字作為唯一鍵。
- [ ] 遊戲資料與 UI 分離；可替換的圖片路徑沒有散落在多處。
- [ ] 既有 `localStorage` key、舊存檔、傭兵／裝備上限與獎勵只發放一次的規則仍被保護。
- [ ] 數值改動有理由，並確認不意外影響其他怪物、裝備或獎勵。
- [ ] 新素材名稱、位置、來源與引用路徑正確，沒有加入不應提交的大型產物或秘密。
- [ ] 錯誤狀態、空資料、重載頁面與重複操作都有合理結果。
- [ ] 對應測試、文件與 `AI_HANDOFF.md` 已在需要時更新。

## 11. 測試檢查表

每次改動至少測試受影響的功能；發布前依序完成完整檢查。既有部分測試可能記錄了過時規則，遇到失敗時先判斷它是否仍代表目前設計，再決定修程式或更新測試。

- [ ] 執行受影響模組的自動測試。
- [ ] 執行 `node --test tests/*.test.mjs`，記錄任何已知／刻意保留的失敗。
- [ ] 執行 `pnpm lint` 與 `pnpm build`。
- [ ] 建立新角色，確認初始流程正常。
- [ ] 載入舊角色並重新整理頁面，確認存檔、背包、傭兵與任務進度沒有遺失。
- [ ] 實際進行至少一場戰鬥，檢查勝負、獎勵、死亡恢復、掉落與自動技能。
- [ ] 若改動隊伍或裝備，檢查招募、上陣、休息、編隊、裝備／卸下與數值更新。
- [ ] 若改動 NPC／任務，檢查對話、條件、獎勵、重複領取與重載後狀態。
- [ ] 在桌面與窄螢幕寬度快速查看 UI 是否可讀、可操作。

## 12. 每日開發流程

開始工作前：

1. 拉取並閱讀最新變更，確認目前分支與工作目標。
2. 閱讀相關資料、元件、測試與 `AI_HANDOFF.md`；尤其是存檔與戰鬥改動。
3. 把今天的工作切成可獨立驗證的小項目。

開發中：

1. 一次只完成一個小項目，例如「新增一個 NPC 對話」或「修正戰鬥獎勵重複發放」。
2. 先讓功能可玩，再補資料驗證、錯誤處理與測試；Alpha 階段可使用 placeholder 美術。
3. 完成後立即測試、review 差異並建立一個符合規範的 commit。

結束工作前：

1. 執行必要檢查，至少確認建置成功或清楚記錄失敗原因。
2. Push 已驗證的 commit 到 GitHub。
3. 在工作紀錄或下一次任務中留下：已完成項目、下一步、測試結果、已知問題與存檔風險。

每天最理想的 Git 歷史範例：

```text
feat: 新增漢陽藥師對話
feat: 新增藥草委託獎勵
fix: 修正藥草委託重載後可重複領取
art: 新增漢陽藥師肖像
chore: 更新 AI 交接文件
```

---

最後更新：2026-09-22。規範應隨專案成熟度調整；有意改變既有流程時，先在團隊中說明原因，再更新本文件。
